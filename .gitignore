# OS
.DS_Store
Thumbs.db

# Obsidian local config (per-device, not shared)
.obsidian/

# Editor
.vscode/
*.swp

# Env / secrets
.env
.env.local

# Node (if tooling added later, e.g. Quartz/MkDocs build tools)
node_modules/
dist/
.cache/

# Python (if data/AI code added later)
__pycache__/
*.pyc
.venv/
venv/
site/

# Large media (track via Git LFS instead if needed)
*.mp4
*.mov
